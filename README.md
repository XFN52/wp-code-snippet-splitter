# WP Code Snippet Splitter

Инструмент для разделения экспортированных сниппетов из плагина WP Code (WordPress) на отдельные файлы по типу кода.

## Описание

Этот скрипт позволяет автоматически разделить JSON-экспорт сниппетов из плагина WP Code на отдельные файлы с соответствующими расширениями (.php, .js, .css, .html). Это упрощает управление кодом, версионирование и редактирование сниппетов во внешних редакторах.

## Особенности

- 📦 Автоматическое разделение сниппетов на отдельные файлы
- 🔧 Поддержка различных типов кода (PHP, JavaScript, CSS, HTML)
- 📝 Правильные расширения файлов на основе типа сниппета
- 🧹 Очистка имён файлов от недопустимых символов
- 🔄 Нормализация переносов строк и форматирования
- ✅ Автоматическое добавление `<?php` для PHP-сниппетов
- 📊 Информативный вывод прогресса обработки

## Требования

- Python 3.6+
- Встроенные библиотеки Python (json, os, re, pathlib)

## Установка

1. Клонируйте репозиторий или скачайте файл `split_snippets.py`:
```bash
git clone https://github.com/yourusername/wp-code-snippet-splitter.git
cd wp-code-snippet-splitter
```

2. Сделайте скрипт исполняемым (опционально, для Linux/Mac):
```bash
chmod +x split_snippets.py
```

## Использование

### Базовое использование

```bash
python split_snippets.py <путь_к_json_файлу>
```

Файлы будут сохранены в директорию `output_snippets` по умолчанию.

### Указание выходной директории

```bash
python split_snippets.py <путь_к_json_файлу> <директория_вывода>
```

### Примеры

```bash
# Базовый пример
python split_snippets.py wpcode-snippets-export-2025-08-25.json

# С указанием выходной директории
python split_snippets.py wpcode-snippets-export-2025-08-25.json my_snippets

# Из другой директории
python split_snippets.py /path/to/export.json ./output
```

## Формат входного JSON

Скрипт ожидает JSON-файл со структурой экспорта WP Code:

```json
[
  {
    "id": 1,
    "title": "Название сниппета",
    "code": "<?php echo 'Hello World';",
    "code_type": "php",
    "location": "everywhere",
    "auto_insert": 1,
    "priority": 10,
    "tags": ["utility", "frontend"],
    "note": "Описание сниппета"
  }
]
```

## Формат выходных файлов

Файлы именуются по шаблону: `{id}_{safe_title}{extension}`

Примеры:
- `1_Custom_Header_Script.php`
- `2_Analytics_Code.js`
- `3_Custom_Styles.css`

## Поддерживаемые типы кода

| Тип кода     | Расширение |
|--------------|------------|
| php          | .php       |
| javascript   | .js        |
| js           | .js        |
| css          | .css       |
| html         | .html      |
| text         | .txt       |
| другие       | .txt       |

## Как экспортировать сниппеты из WP Code

1. В административной панели WordPress перейдите в раздел **WP Code > Code Snippets**
2. Выберите сниппеты, которые хотите экспортировать
3. Нажмите кнопку **Export** для получения JSON-файла
4. Используйте полученный файл с этим скриптом

## Обработка ошибок

Скрипт обрабатывает следующие ошибки:
- Отсутствие входного JSON-файла
- Неверный формат JSON
- Ошибки при создании/записи отдельных файлов
- Пропускает проблемные сниппеты и продолжает обработку

## Лицензия

MIT License

## Участие в разработке

Сообщения об ошибках и запросы на добавление функций приветствуются! Пожалуйста, создавайте issues в репозитории проекта.

## Автор

Создано для упрощения работы с экспортами WP Code плагина.